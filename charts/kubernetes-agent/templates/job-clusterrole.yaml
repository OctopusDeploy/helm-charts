apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "kubernetes-agent.jobClusterRoleName" . }}
rules:
{{- if .Values.jobServiceAccount.clusterRole.rules}}
{{ .Values.jobServiceAccount.clusterRole.rules | toYaml| nindent 2 }}
{{- else }}
- apiGroups:
  - '*'
  resources:
  - '*'
  verbs:
  - '*'
- nonResourceURLs:
  - '*'
  verbs:
  - '*'
{{- end }}