suite: "persistence"
templates:
- templates/nfs-statefulset.yaml
tests:
- it: "is not created when nfs is disabled (default)"
  asserts:
  - hasDocuments:
      count: 0

- it: "should match snapshot"
  set:
    persistence:
      accessModes: ["ReadWriteMany"]
      nfs:
        enabled: true
  asserts:
  - matchSnapshot: {}

- it: "does a thing when nfs is enabled, but accessModes does not have ReadWriteMany"
  set:
    persistence:
      accessModes: ["ReadOnlyMany"]
      nfs:
        enabled: true
  asserts:
  - failedTemplate:
      errorMessage: "`.persistence.accessModes` must contain `ReadWriteMany` when `.persistence.nfs.enabled` is `true`"

- it: sets the emptyDir size to be the value in values
  set:
    persistence:
      accessModes: ["ReadWriteMany"]
      nfs:
        enabled: true
      size: 100Gi
  asserts:
  - equal:
      path: spec.template.spec.volumes[?(@.name == 'octopus-volume')].emptyDir.sizeLimit
      value: 100Gi

- it: uses a backing volume when storageClassName is set
  set:
    persistence:
      accessModes: ["ReadWriteMany"]
      nfs:
        enabled: true
        backingVolume:
          storageClassName: "my-storage-class"
  asserts:
    - equal:
        path: spec.volumeClaimTemplates[0].spec.storageClassName
        value: my-storage-class
    - notExists:
        path: spec.template.spec.volumes[?(@.name == 'octopus-volume')].emptyDir

- it: does not use a backing volume when storageClassName is unset
  set:
    persistence:
      accessModes: ["ReadWriteMany"]
      nfs:
        enabled: true
  asserts:
    - notExists:
        path: spec.volumeClaimTemplates
    - exists:
        path: spec.template.spec.volumes[?(@.name == 'octopus-volume')].emptyDir

- it: uses readWriteOnce access mode for the backing volume when storageClassName is set and no accessModes are specified
  set:
    persistence:
      accessModes: ["ReadWriteMany"]
      nfs:
        enabled: true
        backingVolume:
          storageClassName: "my-storage-class"
  asserts:
    - contains:
        path: spec.volumeClaimTemplates[0].spec.accessModes
        content: ReadWriteOnce

- it: uses the accessModes specified in values for the backing volume when storageClassName is set
  set:
    persistence:
      accessModes: ["ReadWriteMany"]
      nfs:
        enabled: true
        backingVolume:
          storageClassName: "my-storage-class"
          accessModes: ["ReadWriteMany", "ReadWriteOnce"]
  asserts:
    - equal:
        path: spec.volumeClaimTemplates[0].spec.accessModes
        value:
          - ReadWriteMany
          - ReadWriteOnce

- it: sets custom pod spec properly
  set:
    persistence:
      accessModes: ["ReadWriteMany"]
      nfs:
        enabled: true
        spec:
          priorityClassName: "custom-priority-class"
  asserts:
    - equal:
        path: spec.template.spec.priorityClassName
        value: "custom-priority-class"

- it: sets custom container spec properly
  set:
    persistence:
      accessModes: ["ReadWriteMany"]
      nfs:
        enabled: true
        containers:
          nfs:
            spec:
              lifecycle:
                preStop:
                  exec:
                    command: ["/bin/sh", "-c", "echo stopping"]
  asserts:
    - equal:
        path: spec.template.spec.containers[0].lifecycle.preStop.exec.command
        value: ["/bin/sh", "-c", "echo stopping"]

- it: sets custom container env properly
  set:
    persistence:
      accessModes: ["ReadWriteMany"]
      nfs:
        enabled: true
        containers:
          nfs:
            env:
              - name: "CUSTOM_VAR"
                value: "custom-value"
  asserts:
    - contains:
        path: spec.template.spec.containers[0].env
        content:
          name: "CUSTOM_VAR"
          value: "custom-value"