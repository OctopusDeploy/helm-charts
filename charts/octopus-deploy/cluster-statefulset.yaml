apiVersion: apps/v1
kind: StatefulSet
metadata:
  annotations:
    meta.helm.sh/release-name: octopus-deploy
    meta.helm.sh/release-namespace: octopus-deploy
  creationTimestamp: "2026-04-09T12:42:43Z"
  generation: 1
  labels:
    app.kubernetes.io/instance: octopus-deploy
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: octopus-deploy
    helm.sh/chart: octopusdeploy-helm-1.11.0
  name: octopus-deploy
  namespace: octopus-deploy
  resourceVersion: "19021723"
  uid: 95d43143-cf37-4755-96e9-07f3a5da728e
spec:
  persistentVolumeClaimRetentionPolicy:
    whenDeleted: Retain
    whenScaled: Retain
  podManagementPolicy: OrderedReady
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app.kubernetes.io/component: octopus-server
      app.kubernetes.io/instance: octopus-deploy
      app.kubernetes.io/managed-by: Helm
      app.kubernetes.io/name: octopus-deploy
      helm.sh/chart: octopusdeploy-helm-1.11.0
  serviceName: octopus-deploy
  template:
    metadata:
      creationTimestamp: null
      labels:
        app.kubernetes.io/component: octopus-server
        app.kubernetes.io/instance: octopus-deploy
        app.kubernetes.io/managed-by: Helm
        app.kubernetes.io/name: octopus-deploy
        helm.sh/chart: octopusdeploy-helm-1.11.0
    spec:
      containers:
      - env:
        - name: ACCEPT_EULA
          value: "Y"
        - name: OCTOPUS_SERVER_NODE_NAME
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: metadata.name
        - name: DB_CONNECTION_STRING
          valueFrom:
            secretKeyRef:
              key: secret
              name: octopus-deploy-connectionstring
        - name: ADMIN_EMAIL
        - name: TASK_CAP
        - name: OCTOPUS_SERVER_BASE64_LICENSE
          valueFrom:
            secretKeyRef:
              key: secret
              name: octopus-deploy-licensekey
        - name: MASTER_KEY
          valueFrom:
            secretKeyRef:
              key: secret
              name: octopus-deploy-masterkey
        - name: DISABLE_DIND
          value: "Y"
        - name: OCTOPUS_INSTALLED_VIA_HELM
          value: "true"
        - name: OCTOPUS_GRPC_SERVICES_PORT
          value: "8443"
        image: docker.packages.octopushq.com/octopusdeploy/octopusdeploy:2026.2.4992-noamgal-yos-32-investigate-octopus-deploy-helm-chart-compatibility-with-linux
        imagePullPolicy: IfNotPresent
        livenessProbe:
          exec:
            command:
            - /bin/bash
            - -c
            - URL=http://localhost:8080; response=$(/usr/bin/curl -k $URL/api/octopusservernodes/ping
              --write-out %{http_code} --silent --output /dev/null); /usr/bin/test
              "$response" -ge 200 && /usr/bin/test "$response" -le 299 || /usr/bin/test
              "$response" -eq 418
          failureThreshold: 10
          periodSeconds: 30
          successThreshold: 1
          timeoutSeconds: 5
        name: octopus
        ports:
        - containerPort: 8080
          name: web
          protocol: TCP
        - containerPort: 10943
          name: tentacle
          protocol: TCP
        - containerPort: 8443
          name: grpc
          protocol: TCP
        readinessProbe:
          exec:
            command:
            - /bin/bash
            - -c
            - URL=http://localhost:8080; response=$(/usr/bin/curl -k $URL/api/serverstatus/hosted/internal
              --write-out %{http_code} --silent --output /dev/null); /usr/bin/test
              "$response" -ge 200 && /usr/bin/test "$response" -le 299 || /usr/bin/test
          failureThreshold: 60
          initialDelaySeconds: 30
          periodSeconds: 30
          successThreshold: 1
          timeoutSeconds: 5
        resources: {}
        startupProbe:
          exec:
            command:
            - /bin/bash
            - -c
            - URL=http://localhost:8080; response=$(/usr/bin/curl -k $URL/api/octopusservernodes/ping
              --write-out %{http_code} --silent --output /dev/null); /usr/bin/test
              "$response" -ge 200 && /usr/bin/test "$response" -le 299 || /usr/bin/test
              "$response" -eq 418
          failureThreshold: 30
          periodSeconds: 60
          successThreshold: 1
          timeoutSeconds: 1
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
        volumeMounts:
        - mountPath: /repository
          name: package-repository-volume
        - mountPath: /artifacts
          name: artifacts-volume
        - mountPath: /taskLogs
          name: task-log-volume
        - mountPath: /home/octopus/.octopus/OctopusServer/Server/Logs
          name: server-log-volume
        - mountPath: /eventExports
          name: audit-log-volume
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      serviceAccount: default
      serviceAccountName: default
      terminationGracePeriodSeconds: 10
      volumes:
      - name: package-repository-volume
        persistentVolumeClaim:
          claimName: package-repository-claim
      - name: artifacts-volume
        persistentVolumeClaim:
          claimName: artifacts-claim
      - name: task-log-volume
        persistentVolumeClaim:
          claimName: task-log-claim
      - name: audit-log-volume
        persistentVolumeClaim:
          claimName: audit-log-claim
  updateStrategy:
    rollingUpdate:
      partition: 0
    type: RollingUpdate
  volumeClaimTemplates:
  - apiVersion: v1
    kind: PersistentVolumeClaim
    metadata:
      creationTimestamp: null
      name: server-log-volume
    spec:
      accessModes:
      - ReadWriteOnce
      resources:
        requests:
          storage: 200Mi
      volumeMode: Filesystem
    status:
      phase: Pending
