agent:
  acceptEula: "Y"
  name: "#{AgentName}"
  serverCommsAddress: "#{ServerCommsAddress}"
  serverUrl: "https://this.is.not.required.com/"
  bearerToken: "this-is-a-fake-bearer-token"
  space: "Default"
  deploymentTarget:
    enabled: true
    initial:
      environments: ["development"]
      tags: ["testing-cluster"]
  
testing:
  tentacle:
    configMap:
      data: #{ConfigMapData}